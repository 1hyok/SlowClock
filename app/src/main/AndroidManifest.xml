<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 알람이 울리는 동안 소리를 재생하는 포그라운드 서비스의 타입 권한. 없으면
         startForeground 가 SecurityException 으로 죽는다(#122). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- 알람 시각을 정확히 지키기 위한 권한. USE_EXACT_ALARM 은 알람이 본업인 앱에 자동으로
         부여되고 사용자가 회수할 수 없으며, 앱을 오래 안 열어도 대기 버킷이 RESTRICTED 로
         떨어지지 않게 한다. 고령자가 앱을 열지 않고 알람만 받는 이 앱에 필요하다(#122).
         SCHEDULE_EXACT_ALARM 은 USE_EXACT_ALARM 이 없는 API 32 기기에만 쓴다. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 기기를 껐다 켜면 걸어 둔 알람이 전부 취소된다. 다시 걸려면 이 방송을 받아야 한다(#127). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Firebase Analytics 가 병합하는 광고 ID 권한을 뺀다 — 광고가 없고, Play 데이터 보안 양식에서
         광고 ID 수집 항목을 「아니오」로 유지하기 위해서다. Analytics 자체는 광고 ID 없이 동작한다. -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />

    <application
        android:name=".SlowClockApplication"
        android:allowBackup="true"
        android:backupAgent=".backup.SlowClockBackupAgent"
        android:fullBackupOnly="true"
        android:usesCleartextTraffic="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SlowClock">

        <!-- 크래시 수집은 릴리스 빌드에서만 켠다. 값은 buildTypes 의 manifestPlaceholders 가 넣는다(#98). -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${crashlyticsCollectionEnabled}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SlowClock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.alarm.AlarmFullScreenActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:turnScreenOn="true" />

        <!-- 알람이 울리는 동안 소리를 재생한다. shortService 는 시간 제한이 있고 배경 오디오의
             문턱 아래라 알람에 맞지 않는다(#122). -->
        <service
            android:name=".ui.alarm.AlarmTriggerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <service
            android:name=".notification.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="false" />

        <!-- 보호된 시스템 방송만 받는다. exported="false" 여도 시스템은 배달한다(#127). -->
        <receiver
            android:name=".receiver.AlarmRestoreReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>
    </application>

</manifest>